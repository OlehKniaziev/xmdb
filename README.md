# XMDB
XMDB is a media-first relational database which supports most popular video, audio and image codecs as builtin SQL
column types.

## Project structure
- **src/** - database server source code
- **tests/** - database server tests
